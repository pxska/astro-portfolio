---
const { src, alt, description } = Astro.props;
---

<img
  class="object-cover bg-center aspect-video transition max-w-full rounded-md"
  src={src}
  alt={alt}
/>

{
  (description || alt) && (
    <small class="-mt-4 leading-snug">{description || alt}</small>
  )
}
