---
const { src, alt, description } = Astro.props;
---

<img
  class="object-cover bg-center aspect-video transition max-w-full rounded-lg"
  src={src}
  alt={alt}
/>
{(description || alt) && <small class="-mt-6">{description || alt}</small>}
